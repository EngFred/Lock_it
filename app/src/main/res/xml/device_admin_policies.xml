<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Minimal policies; add more if needed (e.g., <force-lock /> for auto-lock) -->
        <limit-password />
        <watch-login />
        <reset-password />
        <wipe-data />
    </uses-policies>
</device-admin>